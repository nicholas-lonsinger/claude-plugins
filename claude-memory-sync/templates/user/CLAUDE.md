# Global instructions — every project, every machine

<!-- Always loaded into every session on every machine. Keep this file
     ruthlessly small: guidance that can be recalled on demand belongs in an
     adjacent memory file plus an index line in MEMORY.md instead. -->
